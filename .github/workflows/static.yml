<!DOCTYPE html>
<html lang="pt-br">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>AstralBot • IA Real</title>

<style>
    :root {
        --bg-main: #0d001a;
        --bg-chat: #120521;
        --accent: #7b2cbf;
        --accent-dark: #5a189a;
        --text-light: #f0e6ff;
        --text-muted: #b19cd9;
        --border: #3c1f5f;
    }

    * { margin:0; padding:0; box-sizing:border-box; }
    body {
        font-family: "Segoe UI", system-ui, sans-serif;
        background: var(--bg-main);
        color: var(--text-light);
        height: 100vh;
        background-image: 
            radial-gradient(circle at 15% 50%, rgba(123,44,191,0.12) 0%, transparent 40%),
            radial-gradient(circle at 85% 30%, rgba(90,24,154,0.10) 0%, transparent 45%);
        background-attachment: fixed;
    }

    .container {
        max-width: 980px;
        height: 100vh;
        margin: 0 auto;
        display: flex;
        flex-direction: column;
    }

    header {
        padding: 16px 20px;
        background: linear-gradient(to bottom, rgba(18,5,33,0.85), rgba(18,5,33,0.95));
        border-bottom: 1px solid var(--border);
        display: flex;
        align-items: center;
        gap: 14px;
        backdrop-filter: blur(8px);
    }

    .logo {
        width: 48px;
        height: 48px;
        background: linear-gradient(135deg, #9d4edd 0%, #5a189a 100%);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 24px;
        font-weight: bold;
        color: white;
        box-shadow: 0 0 20px rgba(123,44,191,0.45);
        border: 2px solid rgba(255,255,255,0.12);
    }

    h1 {
        font-size: 1.55rem;
        background: linear-gradient(to right, #c084fc, #d8b4fe);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
    }

    .chat-container {
        flex: 1;
        overflow-y: auto;
        padding: 20px;
        display: flex;
        flex-direction: column;
        gap: 16px;
    }

    .message {
        max-width: 82%;
        padding: 14px 18px;
        border-radius: 18px;
        line-height: 1.4;
        position: relative;
        animation: fadeIn 0.4s ease-out;
    }

    .user { 
        align-self: flex-end; 
        background: var(--accent); 
        color: white; 
        border-bottom-right-radius: 4px; 
    }

    .bot { 
        align-self: flex-start; 
        background: var(--bg-chat); 
        border: 1px solid var(--border); 
        border-bottom-left-radius: 4px; 
        color: var(--text-light); 
    }

    .message small {
        display: block;
        margin-top: 6px;
        opacity: 0.5;
        font-size: 0.74rem;
    }

    .input-area {
        padding: 16px 20px;
        background: rgba(18,5,33,0.88);
        border-top: 1px solid var(--border);
        backdrop-filter: blur(10px);
    }

    .input-wrapper {
        display: flex;
        gap: 10px;
        background: var(--bg-chat);
        border: 1px solid var(--border);
        border-radius: 50px;
        padding: 8px 12px;
    }

    input[type="text"] {
        flex: 1;
        background: transparent;
        border: none;
        color: var(--text-light);
        font-size: 1.05rem;
        padding: 8px 12px;
        outline: none;
    }

    button {
        background: var(--accent);
        color: white;
        border: none;
        border-radius: 50%;
        width: 44px;
        height: 44px;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all 0.2s;
        font-size: 1.3rem;
    }

    button:hover {
        background: var(--accent-dark);
        transform: scale(1.08);
    }

    .thinking::after {
        content: " •••";
        animation: dots 1.8s infinite;
    }

    @keyframes dots {
        0%,20%  { content: " •"; }
        40%     { content: " ••"; }
        60%,100%{ content: " •••"; }
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(12px); }
        to   { opacity: 1; transform: translateY(0); }
    }

    ::-webkit-scrollbar { width: 8px; }
    ::-webkit-scrollbar-track { background: transparent; }
    ::-webkit-scrollbar-thumb { background: var(--accent); border-radius: 4px; }
</style>
</head>
<body>

<div class="container">
    <header>
        <div class="logo">✧</div>
        <h1>AstralBot</h1>
    </header>

    <div class="chat-container" id="chat"></div>

    <div class="input-area">
        <div class="input-wrapper">
            <input type="text" id="input" placeholder="Pergunte qualquer coisa..." autocomplete="off"/>
            <button onclick="sendMessage()">➤</button>
        </div>
    </div>
</div>

<script>
// ===============================================
//     ASTRALBOT - Agora com IA real (Groq 2026)
// ===============================================

const chat = document.getElementById('chat');
const input = document.getElementById('input');

// ─── CONFIGURAÇÃO IMPORTANTE ──────────────────────────────────────
const GROQ_API_KEY = "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";  // ← Cole sua chave do Groq aqui!
const MODEL = "llama-3.3-70b-versatile"; // Outras boas opções: "mixtral-8x7b-32768", "gemma2-9b-it"
// ────────────────────────────────────────────────────────────────────

const systemPrompt = `
Você é AstralBot, uma IA cósmica, inteligente, direta e um pouco mística.
Responda em português brasileiro de forma natural, fluida e inteligente.
Seja útil, preciso e evite respostas muito genéricas ou enrolação.
Use emojis com moderação ✨
`;

let conversationHistory = [
    { role: "system", content: systemPrompt }
];

function addMessage(text, who = 'bot', thinking = false) {
    const msg = document.createElement('div');
    msg.className = `message ${who}`;
    
    if (thinking) {
        msg.classList.add('thinking');
        msg.textContent = "Pensando";
    } else {
        msg.innerHTML = text.replace(/\n/g, '<br>')
                           .replace(/(https?:\/\/[^\s]+)/g, '<a href="$1" target="_blank">$1</a>');
    }
    
    const time = new Date().toLocaleTimeString([], {hour:'2-digit', minute:'2-digit'});
    msg.innerHTML += `<small>${time}</small>`;
    
    chat.appendChild(msg);
    chat.scrollTop = chat.scrollHeight;
    return msg;
}

async function sendMessage() {
    const text = input.value.trim();
    if (!text) return;

    addMessage(text, 'user');
    input.value = '';

    const loading = addMessage('', 'bot', true);

    try {
        // Adiciona mensagem do usuário ao histórico
        conversationHistory.push({ role: "user", content: text });

        const response = await fetch("https://api.groq.com/openai/v1/chat/completions", {
            method: "POST",
            headers: {
                "Content-Type": "application/json",
                "Authorization": `Bearer ${GROQ_API_KEY}`
            },
            body: JSON.stringify({
                model: MODEL,
                messages: conversationHistory,
                temperature: 0.75,
                max_tokens: 2048,
                stream: false
            })
        });

        if (!response.ok) {
            throw new Error(`Erro ${response.status}: ${response.statusText}`);
        }

        const data = await response.json();
        const answer = data.choices[0].message.content;

        // Adiciona resposta da IA ao histórico
        conversationHistory.push({ role: "assistant", content: answer });

        loading.remove();
        addMessage(answer, 'bot');

    } catch (err) {
        loading.remove();
        addMessage(`Ops... algo deu errado 😓\n\n${err.message}\n\nVerifique se:\n1. A chave API está correta\n2. Tem créditos/limite disponível no Groq`, 'bot');
    }
}

// Suporte ao Enter
input.addEventListener("keypress", e => {
    if (e.key === "Enter") {
        e.preventDefault();
        sendMessage();
    }
});

// Boas-vindas
setTimeout(() => {
    addMessage(`✨ Olá! Eu sou o **AstralBot** agora turbinado com IA de verdade!\n\nPode mandar perguntas difíceis, técnicas, filosóficas, códigos, qualquer coisa...\nEstou pronto 🌌`);
}, 400);

</script>
</body>
</html>
